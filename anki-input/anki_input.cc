#include <iostream>
#include <iomanip>
#include <string>
#include <CLI11>

// determine what kind of flash card it is.
// prompt user for fields
// add wrapper text for input

int main(int argc, char** argv)
{
	int flashcard_type = 0;
	string;
	return 0;
}
